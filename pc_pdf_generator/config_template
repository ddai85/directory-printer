Make this file into a go file and fill in appropriate keys below

package pc_pdf_generator

const (
  configClientID     = "planningcenterclientID"
  configClientSecret = "planning center developer api key"
  configPrivatekey   = "google private key to generate signed url"
  sessionStoreAuth   = "session store auth secret"
  sessionStoreCrypt  = "session store crypt secret"
)
